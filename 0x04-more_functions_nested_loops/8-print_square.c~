#include "main.h"
/**
 *print_saquare - Prints out a string to 10.
 *@size: First variable
 */
void print_square(int size)
{
int l;
int a;
for (l = 1; l <= size; l++)
{
for (a = 1; a <= size; a++)
{
_putchar('#');
}
_putchar('\n');
}
}
