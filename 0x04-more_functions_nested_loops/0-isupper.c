#include "main.h"

/**
 * _isupper - returns the largest of 3 numbers
 * @c: third integer
 * Return: largest number
 */

int _isupper(int c)
{
if (c >= 65 && c < 91)
{
return (1);
}
else
{
return (0);
}
}
