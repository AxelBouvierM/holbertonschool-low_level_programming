#include "main.h"

/**
 * mul - returns the largest of 3 numbers
 * @c: third integer
 * Return: largest number
 */

int mul(int a, int b)
{
int mul;
int result;

mul = a * b;
result = mul;
return (result);
}
