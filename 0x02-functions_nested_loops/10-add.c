#include "main.h"
/**
 *add - Prints out a string to 10.
 *Return: 0 if success.
 *@a: Variable.
 *@b: Variable.
 */
int add(int a, int b)
{
int sum;
int result;
sum = a + b;
result = sum;
return (result);
}
